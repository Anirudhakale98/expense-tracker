# MongoDB Atlas connection string (required)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/expense-tracker

# Optional: protect API routes (set any secret string)
API_SECRET=

# Monthly salary for budget tracking (optional, can also set in app)
DEFAULT_SALARY=
